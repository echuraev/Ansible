---
- name: Install packages
  package:
    name: "{{ item }}"
    state: installed
  with_items:
    - zsh
    - git
    - vim
    - mc
    - tmux
    - cmake
    - python-pip
    - openssh-server
    # google-chrome
    # dropbox
  become: yes
  become_user: root
